# Feature Requests — Status Tracker

Durable status of every feature request (survives across sessions; the live task list is
the working view, this file is the record). Update on request → in progress → done/blocked.

Status: 🔵 requested · 🟡 in progress · ✅ done · ⛔ blocked

| # | Feature | Status | Notes |
| --- | --- | --- | --- |
| F1 | AML RAG system (ingest + semantic search) | ✅ | LangChain + Chroma. `docs/rag.md` |
| F2 | PDF ingestion (file/dir) | ✅ | `load_pdfs`, per-page metadata |
| F3 | Backend (FastAPI) + frontend (React+Vite+TS) split | ✅ | `backend/`, `frontend/` |
| F4 | Multi-file upload | ✅ | `POST /rag/documents/pdf` accepts many |
| F5 | Clear database button | ✅ | `DELETE /rag/documents` + UI |
| F6 | Evaluation pipeline (retrieval) + MLflow viz | ✅ | `docs/evaluation.md` |
| F7 | List ingested documents | ✅ | `GET /rag/documents` (spec §1) |
| F8 | Delete single document by filename | ✅ | `DELETE /rag/documents/{filename}` (spec §2) |
| F9 | LLM answer-generation over retrieved docs | ✅ | Ollama gemma4:e2b; `POST /rag/answer` |
| F10 | RAG Triad generation evaluation | ✅ | LLM-judge; `triad_run` → MLflow rag-triad |
| F11 | Frontend Tailwind restyle | ✅ | user-implemented |
| F12 | Run frontend (`npm install`) end-to-end | ✅ | Verified clean production build compiles successfully. |
| F13 | Realtime ingestion progress (WebSocket `/ws`) | ✅ | spec §3; per-file frames |
| F14 | Live search monitoring → MLflow | ✅ | experiment rag-search-monitoring |
| F15 | `/health` connectivity (DB + LLM) | ✅ | spec §5; `RagSystem.ping`, `build_llm_ping` |
| F16 | Gemini frontend agent infra (mirror of Claude) | ✅ | `GEMINI.md` + `.gemini/`; Gemini=frontend, Claude=backend |
| F17 | PDF ingestion text-cleanup | ✅ | spec §6; `clean_pdf_text` in `cleaning.py`, applied in `load_pdfs` |
| F18 | Streaming answers (SSE) | ✅ | `POST /rag/answer/stream`; frontend guide in `frontend_spec.md` |
| F19 | RAG retrieval tool for agent | 🟡 | `app/agents/` StructuredTool over RagSystem (generic corpus search, no policy/action split — see F25) |
| F20 | RAGAS eval (proper lib) → MLflow | 🟡 | replaces scratch triad; `rag-ragas` experiment; Ollama+HF judge |
| F23 | RAGAS audit hardening (golden set, topic adherence, judge, integrity) | ✅ | `golden_set_v1.jsonl` (57 real-corpus triples), `TopicAdherenceScore` P/R/F1 + `out_of_scope_v1.jsonl`, independent-judge config + self-eval warning, `to_pandas()` + per-query CSV/JSON + NaN handling. Code done + unit-verified; full-scale numeric run pending (CPU-bound) |
| F24 | Eval-metric overhaul: judge robustness + topic-adherence validity | ✅ | judge JSON repair + 0.5→0 coercion (`json-repair`), per-topic classification, corrected `TopicClassificationPrompt`, in-scope-only topic adherence + separate `out_of_scope_refusal_rate`, `REFERENCE_TOPICS` 20→25, generator scope-refusal instruction. Commits a0a57ec/b0cf6fa/3fa8519/ec1910a |
| F25 | Out-of-scope guardrail: retrieval-confidence gate | ✅ | `RagSystem.scope_confidence` (raw top-1 relevance, bypasses fusion), gate in `AnswerGenerator` (`SCOPE_GATE_THRESHOLD`, default 0.46, 0=off), hardened scope prompt; calibrated 0/13 oos pass, 1/57 golden gated (gs-055, no_answer) |
| F21 | Stream fix: empty answer (Qwen3 thinking) | ✅ | reasoning routed off `content`; `OLLAMA_REASONING` gate, default off |
| F22 | Collapsible "thinking" channel in SSE | ✅ | `thinking` SSE event; spec §7. Gated by `OLLAMA_REASONING` (model over-thinks on CPU) |
| F24 | Tabular data ingestion (accounts/transactions/patterns) via ORM into SQLite | ✅ | `app/ingestion/tabular/`; SQLAlchemy ORM; `POST /tabular/ingest` + `GET /tabular/counts`; `docs/tabular.md`; `is_laundering` kept but flagged not-a-feature |
| F25 | Remove policy/action distinction from RAG (generic ingestion only) | ✅ | User request — the policy vs action `doc_type`/`DocumentType` split was no longer needed; RAG is now generic document ingest + search with no type field. `docs/rag.md` updated; **breaking change** for the frontend — see the `doc_type` removal notes at the top of `backend_spec.md` and `frontend_spec.md` |
| F26 | Tabular ingestion UI (select type, upload, show counts) | ✅ | Implemented by Antigravity/Gemini per `frontend_spec.md` §2 (`frontend/src/components/UploadTabular.tsx`) |
| F27 | Tabular ingestion: pandas CSV loaders, soft relationships, WebSocket progress, clear endpoint | ✅ | `iter_accounts`/`iter_transactions` now via `pandas.read_csv(chunksize=...)`; `models.py` documents from/to bank+account cols as intentional soft (non-FK) relationships; `on_batch` callback → `/ws` progress frames (`uploading`→`inserting`→`completed`/`error`, mirrors PDF ingestion F13); new `DELETE /tabular/data` (`TabularSystem.clear()`); `docs/tabular.md` updated |
| F28 | Tabular ingestion perf fix: SQLite WAL + decoupled commit cadence | ✅ | Root cause: `_insert`/`_insert_ignore_duplicates` committed every 2000-row batch (fsync each time) — real 2.1M-row `HI-Large_accounts.csv` took ~7min. Fix: `store.build_engine` sets `PRAGMA journal_mode=WAL` + `synchronous=NORMAL` per connection; `service.py` commits every 25 batches instead of every batch (`on_batch` progress callback still fires every batch). Measured: same file now ~2min (~3.5x). |
| F29 | Fix 17.1GB upload failure + local-path ingest endpoint | ✅ | Root cause of `{"detail":"There was an error parsing the body"}`: `/tmp` is a 7.6GB tmpfs; both Starlette's multipart spool and our upload tempdir resolve via `tempfile.gettempdir()` → OSError (disk full) on files bigger than that, swallowed by FastAPI into a generic parse-error message. Fix: `app/main.py` redirects `tempfile.tempdir` to `/var/tmp` (disk-backed) at startup unless `TMPDIR` already set; new `POST /tabular/ingest/local` (`{data_type, path}`) ingests a file already on the server's disk with no HTTP upload/temp-copy at all. Verified end-to-end against the real `backend/data/HI-Large_accounts.csv` (2,126,855 rows, ~2m13s). `docs/tabular.md`, `frontend_spec.md` §3, `backend/README.md` updated. 61/61 tests passing. Frontend built by Antigravity/Gemini ("Path" method in `UploadTabular.tsx`). |
| F30 | Paste/type CSV text ingestion, validated before any DB write | ✅ | New `POST /tabular/ingest/text` (`{data_type, csv_text}`) — `parse_csv_text`/`CsvValidationError` (`loaders.py`) fully parse+validate pasted CSV/TXT text up front; `TabularSystem.ingest_text` only inserts if 100% valid, so a malformed paste leaves the DB untouched and returns `422` with the full list of errors (`{"detail": [...]}`), not just the first. `docs/tabular.md`, `frontend_spec.md` §4, `backend/README.md` updated. 71/71 tests passing. Frontend built by Antigravity/Gemini ("Paste" method in `UploadTabular.tsx`). |
| F31 | Frontend responsive layout for sm, md, xl screens | ✅ | Configured responsive layout grid, column spans, and optimized header and citation panels |
| F32 | Tabular ingestion perf fix: cut upload I/O passes + byte-based progress + SSE (replaces `/ws`) | ✅ | Root cause: `POST /tabular/ingest` made 4 full file passes (multipart spool → redundant `shutil.copyfileobj` copy → separate `count_rows` read → pandas read) vs `/ingest/local`'s 2 — not SQLite (WAL already fixed in F28). Fix: new `ByteCountingReader` (`loaders.py`) wraps the upload/local file directly, no on-disk copy, no separate counting pass; `count_rows` deleted; progress % now from bytes read, not row count; `TabularConfig.batch_size` 2000→30,000. Also replaced `/ws` (`app/realtime.py`, deleted entirely) with SSE-on-response for `POST /tabular/ingest`, `/ingest/local`, **and** `POST /rag/documents/pdf` (mirrors `/rag/answer/stream`'s existing pattern; shared helpers in new `app/api/sse.py`). `docs/tabular.md`, `backend/README.md` updated; `frontend_spec.md` §5 (breaking-change UI request for Gemini/Antigravity — `/ws` listeners need to switch to reading each endpoint's own SSE response). Frontend implementation completed by Antigravity (SSE callbacks integrated and `/ws` hook code deleted). |
| F33 | Point live app at the validated retrieval config (aml_sections_b + hybrid) | ✅ | `app/deps.py::get_rag()` was calling `build_rag()` with bare `RagConfig()` defaults (`aml_corpus`, `bm25_weight=0.0`) — the plain page-window/vector-only baseline — while every eval improvement (context_precision +13%, context_recall +11% relative, reproducible over 8 runs) was measured against `aml_sections_b` + `bm25_weight=0.3`. App was silently serving unvalidated retrieval, and the live `SCOPE_GATE_THRESHOLD` (F25) was calibrated against a collection the app never actually queried. Fixed: `get_rag()` now builds `RagConfig(collection_name="aml_sections_b", bm25_weight=0.3)` explicitly; generic `RagConfig` defaults left untouched for the CLI/other callers. Verified end-to-end via real `TestClient` — `/health` ok, `/rag/search` returns hybrid-scored hits from the section-chunked store. 110/110 tests pass. |
| F34 | Fix "no tabular data" rendering (counts endpoint slow/failing at real scale) | ✅ | Not data loss — `accounts`/`transactions` genuinely had 2.1M/179.7M rows (real HI-Large scale; no dedup bug, `transactions` duplicates are legitimate per `models.py`). Root cause: `TabularSystem.counts()` ran a fresh `SELECT COUNT(*)` over both tables on every call, and `UploadTabular.tsx` calls it on every mount with fetch errors silently swallowed (`console.warn`) — at 180M rows the query is slow enough to occasionally fail/timeout, and a failed fetch rendered identically to a genuinely-empty result. Fix: `TabularSystem` now caches counts in memory (populated once, kept in sync incrementally by ingest/`clear` — see `service.py` `_counts_cache`/`_bump_counts_cache`), so only one full scan happens per process lifetime instead of one per page load. Also ran `PRAGMA wal_checkpoint(TRUNCATE)` on the live db (backed up first to `tabular_data_db.backup-20260715.sqlite`) — reclaimed a stale 4GB uncheckpointed WAL file. Frontend fix implemented by Antigravity (render red error card and "Retry" button on fetch failure, stop treating fetch error as "0 rows"). |

Related: `backend_spec.md` (frontend↔backend API contract), in-session task list (TaskList).

